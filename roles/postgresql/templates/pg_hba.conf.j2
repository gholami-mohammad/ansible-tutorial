local   all                         postgres                                peer

# TYPE  DATABASE                    USER            ADDRESS                 METHOD
local   all                         all                                     peer

# IPv4 local connections:
host    {{ db_name }}             {{ db_username }}    127.0.0.1/32         {{ db_pass_encryption }}

# IPv6 local connections:
host    {{ db_name }}             {{ db_username }}    ::1/128              {{ db_pass_encryption }}

local   replication                 all                                     peer
host    replication                 all             127.0.0.1/32            {{ db_pass_encryption }}
host    replication                 all             ::1/128                 {{ db_pass_encryption }}
